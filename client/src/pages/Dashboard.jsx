// DEPRECATED — superseded by pages/app/Routine.jsx, which is now the
// homepage inside the header+footer app shell (AppLayout.jsx). Not imported
// anywhere. Kept only because the sandbox couldn't delete this file from
// the mounted project folder — safe to delete manually.
export default null
